# Example keystore.properties file
# Copy this to keystore.properties and fill in your actual values
# DO NOT commit keystore.properties to version control!

# Path to your keystore file (relative to project root or absolute path)
storeFile=../path/to/aikeyboard-release.jks

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

# Key alias (the alias you used when creating the keystore)
keyAlias=aikeyboard

# Key password (usually same as keystore password)
keyPassword=YOUR_KEY_PASSWORD_HERE

