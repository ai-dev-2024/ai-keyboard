<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".AIKeyboardApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIKeyboard"
        tools:targetApi="31">
        
        <!-- IME Service -->
        <service
            android:name=".ime.AIKeyboardService"
            android:label="@string/keyboard_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/Theme.AIKeyboard"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/settings_title"
            android:exported="true"
            android:theme="@style/Theme.AIKeyboard">
            <intent-filter>
                <action android:name="android.settings.INPUT_METHOD_SETTINGS" />
            </intent-filter>
        </activity>

        <!-- ASR Testing Activity -->
        <activity
            android:name=".ui.asrtesting.ASRTestingActivity"
            android:label="ASR Engine Testing"
            android:exported="false"
            android:theme="@style/Theme.AIKeyboard" />

        <!-- Voice Input Service -->
        <service
            android:name=".voiceinput.service.VoiceInputService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>

